[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NEXT_SKIP_VALIDATION = "1"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
