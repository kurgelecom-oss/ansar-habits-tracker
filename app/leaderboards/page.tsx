"use client";import{useEffect,useState}from"react";import ClubNavigation from"../components/dashboard/ClubNavigation";import styles from"./leaderboards.module.css";type Row={position:number;team:string;points:number;played:number;goalDifference:number};type Table={name:string;code?:string;table:Row[]|null};export default function Leaderboards(){const[data,setData]=useState<{available:boolean;tables:Table[];message?:string}|null>(null);const[active,setActive]=useState("");useEffect(()=>{fetch("/api/football/real-madrid/standings").then(r=>r.json()).then(x=>{setData(x);setActive(x.tables?.[0]?.name||"")}).catch(()=>setData({available:false,tables:[],message:"Competition tables are unavailable."}))},[]);const table=data?.tables.find(t=>t.name===active);return <main className={styles.page} aria-label="ANSAR OS Leaderboards"><ClubNavigation activeLabel="Leaderboards"/><section className={styles.content}><header><p>REAL MADRID · MATCH CENTRE</p><h1>Competition tables</h1><span>Every current competition available through the live football feed.</span></header>{!data?<p className={styles.loading}>Loading Real Madrid season tables…</p>:!data.available?<p className={styles.loading}>{data.message}</p>:<><nav className={styles.tabs}>{data.tables.map(t=><button onClick={()=>setActive(t.name)} className={active===t.name?styles.active:""} key={t.name}>{t.name}</button>)}</nav>{table?.table?<section className={styles.board}><div className={styles.head}><h2>{table.name}</h2><span>Pts · Pld · GD</span></div>{table.table.map(row=><div className={row.team.toLowerCase().includes("real madrid")?styles.madrid:styles.row} key={row.position}><b>{row.position}</b><strong>{row.team}</strong><span>{row.points}</span><span>{row.played}</span><span>{row.goalDifference>0?`+${row.goalDifference}`:row.goalDifference}</span></div>)}</section>:<section className={styles.empty}><h2>{table?.name}</h2><p>This competition does not publish a league table through the current feed. Cup progress remains match-based.</p></section>}</>}</section></main>}
