"use client";import{useEffect,useState}from"react";import ClubNavigation from"../components/dashboard/ClubNavigation";import styles from"./leaderboards.module.css";type Row={position:number;team:string;points:number;played:number;goalDifference:number;[k:string]:any};type Table={name:string;table:Row[]|null};export default function Leaderboards(){const[data,setData]=useState<{available:boolean;tables:Table[];message?:string}|null>(null);const[active,setActive]=useState("");const[view,setView]=useState("Standings");useEffect(()=>{fetch("/api/football/real-madrid/standings").then(r=>r.json()).then(x=>{setData(x);setActive(x.tables?.[0]?.name||"")}).catch(()=>setData({available:false,tables:[],message:"Competition tables are unavailable."}))},[]);const table=data?.tables.find(t=>t.name===active);return <main className={styles.page} aria-label="ANSAR OS Leaderboards"><ClubNavigation activeLabel="Leaderboards"/><section className={styles.content}><header><p>REAL MADRID · SEASON CENTRE</p><h1>{active||"Competition tables"}</h1><span>Live competition data · standings, results and season numbers.</span></header>{!data?<p className={styles.loading}>Loading season data…</p>:!data.available?<p className={styles.loading}>{data.message}</p>:<><nav className={styles.tabs}>{data.tables.map(t=><button onClick={()=>setActive(t.name)} className={active===t.name?styles.active:""} key={t.name}>{t.name}</button>)}</nav><nav className={styles.views}>{["Matches","News","Standings","Stats"].map(v=><button onClick={()=>setView(v)} className={view===v?styles.active:""} key={v}>{v}</button>)}</nav>{view==="Standings"&&table?.table?<section className={styles.board}><div className={styles.head}><span>Team</span><span>MP</span><span>W</span><span>D</span><span>L</span><span>GF</span><span>GA</span><span>GD</span><b>PTS</b></div>{table.table.map(r=><div className={r.team.toLowerCase().includes("real madrid")?styles.madrid:styles.row} key={r.position}><b>{r.position}</b><strong>{r.team}</strong><span>{r.played}</span><span>{r.won??"—"}</span><span>{r.draw??"—"}</span><span>{r.lost??"—"}</span><span>{r.goalsFor??"—"}</span><span>{r.goalsAgainst??"—"}</span><span>{r.goalDifference}</span><b>{r.points}</b></div>)}</section>:<section className={styles.empty}><h2>{view}</h2><p>{view==="News"?"The live provider has no editorial news feed, so this OS will not invent stories.":view==="Matches"?"Fixtures and results remain in the Dashboard Match Centre until the full fixture feed is enabled.":view==="Stats"?"The current provider endpoint does not publish competition statistics.":"This competition has no published league table."}</p></section>}</>}</section></main>}
